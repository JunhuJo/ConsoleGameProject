<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="AllGameMain.cs" />
    <Compile Remove="Player(예비).cs" />
    <Compile Remove="Player(최신예비).cs" />
  </ItemGroup>

  <ItemGroup>
    <None Include="Player%28최신예비%29.cs" />
  </ItemGroup>

</Project>
